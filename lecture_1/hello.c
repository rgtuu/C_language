#include <stdio.h> // STanDard Input Output 표준 입출력, 헤더파일

int main() { // main에서 첫 번째로 실행할 명령어의 주소값이 전달된다. -> main이라는 LABEL이 있는 주소값을 알려주는 역할
    printf("Hello, World! \n"); //main을 실행하였을 때 중괄호 안에 있는 것들이 실행된다.
    return 0; //무사히 종료되었음을 알림
}
